# Variation: P12 + F1 (No Early Stopping)
# Base: v_p12 ; with the early-stopping rule disabled — runs all 20 rounds regardless of confidence.
# Achieved by setting high_confidence_threshold to 105 (unreachable).

name: v_p12_f1
description: "P12 base + F1 (no early stopping): runs all 20 rounds regardless of confidence"
workflow_type: dialog_only

agents:
  - patient_agent
  - doctor_q_agent
  - diagnosis_dialog_agent
  - judge_agent

max_rounds: 20
high_confidence_threshold: 105
num_cases: 

# Variation flags
pass_turn_info: true
ask_demographics_first: true
pass_reasoning_to_diff: false
pass_asked_questions: false


# Per-agent model overrides. Uncomment and edit to swap individual agents off
# the default (gpt-4o on OpenAI). patient_agent and judge_agent are intentionally
# omitted as they are not part of the evaluation — keep them on the default model.
# agent_models:
#   doctor_q_agent:
#     provider: grok
#     model: grok-4-1-fast-non-reasoning
#   diagnosis_dialog_agent:
#     provider: grok
#     model: grok-4-1-fast-non-reasoning
