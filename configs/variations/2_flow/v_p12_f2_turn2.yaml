# Variation: P12 + F2 turn2 (Differential Q from round 2)
# Base: v_p12 ; with the differential question agent active from round 2 onward (diagnosis reasoning passed in).

name: v_p12_f2_turn2
description: "P12 base + F2 (differential Q from round 2, with diagnosis reasoning)"
workflow_type: dialog_only_diff

agents:
  - patient_agent
  - doctor_q_agent
  - doctor_q_diff_agent
  - diagnosis_dialog_agent
  - judge_agent

max_rounds: 20
high_confidence_threshold: 95

# Prompt flags (best prompt config from P experiments)
pass_turn_info: true
ask_demographics_first: true
pass_reasoning_to_diff: true
pass_asked_questions: false

# Flow flags
diff_start_round: 2


# Per-agent model overrides. Uncomment and edit to swap individual agents off
# the default (gpt-4o on OpenAI). patient_agent and judge_agent are intentionally
# omitted as they are not part of the evaluation — keep them on the default model.
# agent_models:
#   doctor_q_agent:
#     provider: grok
#     model: grok-4-1-fast-non-reasoning
#   doctor_q_diff_agent:
#     provider: grok
#     model: grok-4-1-fast-non-reasoning
#   diagnosis_dialog_agent:
#     provider: grok
#     model: grok-4-1-fast-non-reasoning
